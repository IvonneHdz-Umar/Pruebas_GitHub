//Apuntes de git
git init	inicializar un proyecto nuevo d egit
gid add.	Agrega lso archivos a git(el punto  indica a todos los archivos, en caso de solo querer solo 1 archivo cambiar el punto por el nombre del archivo)
git commit -m "mensaje"		comando para indicar los cambios realizafos
git push 	subir al repositorio remoto


git pull	 bajar cambios del repositorio remoto
git status	ver el estado del respo local
git branch	 ver las ramas que existen
git branch "rama"	 crear una nueva rama
git checkout -b "rama"	cambiar a la rama seleccionadag

git restore . 		restaura archivos del working
git restore -- staged . restaura archivos del stating 
